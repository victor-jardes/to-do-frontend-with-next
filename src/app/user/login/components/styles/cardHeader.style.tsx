import styled from "styled-components";

export const StyledCardHeader = styled.section`
  color: #000;
  font-size: 30px;
  font-style: normal;
  font-weight: 800;
  line-height: normal;
  letter-spacing: 3px;
  margin: auto;
`;
